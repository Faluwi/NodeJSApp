apiVersion: v1
kind: Service
metadata:
  name: {{ include "myexamplechart.fullname" . }}
  labels:
    {{- include "myexamplechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myexamplechart.selectorLabels" . | nindent 4 }}
