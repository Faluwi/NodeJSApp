apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myexamplechart.fullname" . }}-test-connection"
  labels:
    {{- include "myexamplechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myexamplechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
